{
  "name": "@executor-v2/runtime-cloudflare-worker-loader",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bunx vitest run --no-watch src/*.integration.test.ts",
    "typecheck": "bunx tsc --noEmit -p tsconfig.json",
    "worker:dev": "bunx wrangler dev --local --config worker/wrangler.jsonc",
    "worker:deploy": "bunx wrangler deploy --config worker/wrangler.jsonc"
  },
  "dependencies": {
    "@effect/platform": "catalog:",
    "@executor-v2/engine": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "vitest": "catalog:",
    "wrangler": "^4.0.0"
  }
}